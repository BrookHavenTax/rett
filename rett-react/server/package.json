{
  "name": "rett-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Backend proxy for the RETT React app. Holds the Gemini API key server-side and exposes /api/gemini/extract-w2.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "multer": "^2.1.1"
  }
}
